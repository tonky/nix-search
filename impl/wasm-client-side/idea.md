can we build and package this with wasm as a static page using storage api(or similar), so it would run in browser entirely client side? investigate for moder approaches as of april 2026

not the CLI, but UI part - a 2 pane page with fuzzy search for packages and a button to manually refresh cache

and host using github pages as OSS project

we'll probably have to split this repo and reuse parts of it in the wasm/static page deployment.